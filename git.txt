echo "# BACKEND" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/venkat-988/BACKEND.git
git push -u origin main

  git config --global user.email "venkatmunagala988@gmail.com"
  git config --global user.name "venkat-988"

to set your account's default identity.
Omit --global to set the identity only in this repository.
